---
export const prerender = false;

import Header from '../../components/Header.astro';
import Sidebar from '../../components/admin/Sidebar.astro';
import ProjectList from '../../components/admin/projects/ProjectList.astro';
import ProjectFilter from '../../components/admin/projects/ProjectFilter.astro';

// Auth check
const authCookie = Astro.cookies.get('auth_token');
if (!authCookie || authCookie.value !== 'authorized') {
  return Astro.redirect('/admin/login', 302);
}
---

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Management - Nayuki Blog Admin</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <Header />
    
    <main class="admin-container">
        <Sidebar />

        <div class="content-area">
            <div class="page-header">
                <h2>Project Management</h2>
                <button id="btn-new-project" class="btn-primary"><i class="fas fa-plus"></i> New Project</button>
            </div>

            <div class="filters-container">
                <ProjectFilter />
            </div>

            <ProjectList />
        </div>
    </main>

    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #ffffff;
        }

        .admin-container {
            display: flex;
            min-height: calc(100vh - 60px);
        }

        .content-area {
            flex-grow: 1;
            overflow-x: hidden;
            padding: 20px;
        }

        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .page-header h2 {
            margin: 0;
            color: #303133;
            font-size: 24px;
        }

        .filters-container {
            margin-bottom: 20px;
            max-width: 400px; /* Limit width of filter */
        }

        .btn-primary {
            background: #000000;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: background 0.3s;
        }

        .btn-primary:hover {
            background: #333333;
        }
    </style>
</body>
</html>
